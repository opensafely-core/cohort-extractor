*! version 1.0.1  21nov2004
version 8

class {
} , inherit(stretchability)


program new
	.current = 0				// Null by default

	if `"`0'"' != `""' {
		.Super.new `0'
	}

end

// --------------------------------------------------------------------------
//  Returns the name of the current code
//
//	Usage:  .setting

program setting

	if 0`.current' {
		class exit .codes[`.current']
	}
end
