*! version 1.0.0  30jul2002
version 8

class {

    class:
	codes = { "fixed", "free", "shared" }

} , inherit(fixedcode)


program define new			/* yucky way to get a default */
	.current = 1

	if `"`0'"' != `""' {
		.Super.new `0'
	}

end
