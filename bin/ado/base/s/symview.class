/*                              symview

	A symbol/point in a full view for inclusion in containers that 
	require size to be returned.

*/
*! version 1.0.0  30jul2002

version 8

class {
	.style = .markerstyle.new
} , inherit(view)


/* -------------------------------------------------------------------------*/
program draw

	syntax [ , xsize(real 0) ysize(real 0) ]

	.style.setgdifull

	gdi point `=`xsize'/2' `=`ysize'/2'

end

/* -------------------------------------------------------------------------*/
program xsize

	class exit = max(0`.style.size.gmval', 0`.style.backsize.gmval')
end

/* -------------------------------------------------------------------------*/
program ysize

	class exit = max(0`.style.size.gmval', 0`.style.backsize.gmval')
end
