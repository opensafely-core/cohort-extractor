{* *! version 1.0.0  22jun2019}{...}
{synopt :{cmdab:sel:ection(plugin)}}use a plugin iterative formula to select an optimal value of the lasso penalty parameter lambda* for each lasso; the default{p_end}
{synopt :{cmdab:sel:ection(cv)}}use CV to select an optimal value of the lasso penalty parameter lambda* for each lasso{p_end}
{synopt :{cmdab:sel:ection(adaptive)}}use adaptive lasso to select an optimal value of the lasso penalty parameter lambda* for each lasso{p_end}
