{* *! version 1.0.2  20jan2013}{...}
{phang}
{opt vce(vcetype)} specifies the type of standard error reported, which 
includes types that allow for intragroup correlation ({cmd:cluster}
{it:clustvar}) and that use bootstrap or jackknife methods
({cmd:bootstrap}, {cmd:jackknife}); see
{helpb vce_option:[R] {it:vce_option}}.
{p_end}
