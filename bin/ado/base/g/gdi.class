/*
	Class system binding to the GDI commands
*/
*! version 1.0.3  26aug2019

version 8

class {
}

					/* gdi commands to add if needed */

program define gmoveto
end

program define glineto
end

					/* Initialization and closure */

program define init
	gdi init `0', live
end

program define end
	gdi end `0'
end
					/* Setting registers */

program define set
	gdi `0'
end

/*
program define setxbeta
	gdi xbeta = `0'
end

program define setybeta
	gdi ybeta = `0'
end
*/

					/* Drawing */

program define update
	gdi update `0'
end

program define lineto
	gdi lineto `0'
end

program define moveto
	gdi moveto `0'
end


