*! version 1.0.0  30jul2002
version 8

class {
	textstyle = .textstyle.new   /* , style(small_label) */
	position  = .above_below.new /* , style(above) */
	xoffset   = .gsize.new       /* , style(zero) */
	yoffset   = .gsize.new       /* , style(zero) */
	format    = "%8.3g"

} , inherit(style)

program new
	version 8

	.textstyle.setstyle , style(small_label)
	.position.setstyle  , style(above)
	.xoffset.setstyle   , style(zero)
	.yoffset.setstyle   , style(zero)
end
