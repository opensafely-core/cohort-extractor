{smcl}
{* *! version 1.1.1  11feb2011}{...}
{* this hlp file called by test.dlg}{...}
{vieweralsosee "[R] test" "help test"}{...}
{title:Examples of linear expressions for test}

    {cmd:region1 = region2 = region3}

    {cmd:region4 = (region1+region2)/2}

    {cmd:educ=.1}

    {cmd: x + y - z/2 = 3*a - b}

    {cmd: x + y - z/2 = 3*a - b = m + n - p}

    {cmd: x + y - z/2 = 3*a - b = m + n - p = 4*w + 2*v}
