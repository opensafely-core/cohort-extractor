{* *! version 1.0.0  22jun2019}{...}
{synopt :{opt all:lambdas}}fit models for all lambdas in the grid or until
the {opt stop(#)} tolerance is reached; by default, the CV function is
calculated sequentially by lambda, and estimation stops when a minimum is
identified{p_end}
