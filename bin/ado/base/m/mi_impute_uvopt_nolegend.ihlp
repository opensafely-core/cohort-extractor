{* *! version 1.0.2  03apr2011}{...}
{opt nolegend} suppresses all legends that appear before the imputation
table.  Such legends include a legend about conditional imputation that
appears when the {opt conditional()} option is specified and group legends that
may appear when the {opt by()} option is specified.
