*! version 1.0.0  31jul2002
version 8

class {

} , inherit(codestyle)



// ---------------------------------------------------------------------------

program _unabbrev

	local 0 , `0'

	syntax [ , SCatter LIne COnnected Bar * ]

	if `"`options'"' != `""' {
		class exit `"`options'"'
	}

	class exit "`scatter'`line'`connected'`bar'"

end

