{* *! version 1.0.0  27feb2019}{...}
{p2coldent:* {opth l:evel(numlist)}}confidence level; default is
  {cmd:level(95)}{p_end}
{p2coldent:* {opth a:lpha(numlist)}}significance level; default is 
  {cmd:alpha(0.05)}{p_end}
{p2coldent:* {opth probw:idth(numlist)}}probability of CI width; required to 
  compute sample size and CI width{p_end}
{p2coldent:* {opth w:idth(numlist)}}CI width; required to compute
  sample size and probability of CI width{p_end}
