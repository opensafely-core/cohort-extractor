*! version 1.0.0  03may2019
version 16

class fpcolumn_mean1 {

} , inherit(fpcolumn)

program .new
	syntax [ , Style(passthru) *]

	if `"`style'"' == "" {
		local style "style(scheme fpmean1)"
	}

	.labels = `""Treatment" "Mean""'

	.Super.new , `macval(style)' `macval(options)'
end

program .fill_varidx
	.varidx[1] = `.serset.seriesof _meta_mean1'
	.GetFormat format : _mean1
	.formats[1] = `"`format'"'
end

exit
