*! version 1.0.0  10may2019
version 16

class {

} , inherit(fpcolumn_spacer)

program .new
	syntax [ , Style(passthru) *]

	if `"`style'"' == "" {
		local style "style(scheme fpspacerlb)"
	}

	.labels = `""'

	.Super.new , `macval(style)' `macval(options)'
end

exit
