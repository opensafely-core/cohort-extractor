// function_g
//
// Base class for the function view.

*! version 1.1.0  29may2007

version 9

class {
	type	= .functiontype.new,	     style(line)
} , inherit(yxview)



// ----------------------------------------------------------------------------
// Determine if a position is in/on the view

program _at_pos_function

	if ("`.basestyle'" == "")  class exit = `._at_pos_line `0''
	else			   class exit = `._at_pos_`.basestyle' `0''
end


// ---------------------------------------------------------------------------
// Return the dialog type for the view/plot.  Note, dialog
// files have "gr_ed_" prefixed to this returned value.

program dialog

	if "`.object_found_dialog'" != "" {
		class exit "`.Super.dialog'"
	}
	else if ("`.basestyle'" == "") {
		class exit "line"
	}
	else {
		class exit "`.basestyle'"
	}
end


// ---------------------------------------------------------------------------
// Sort the serset on the x variable.  This is null op for functions.

program SortOnX
	capture di ""
end
