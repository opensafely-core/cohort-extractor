{* *! version 1.0.0  22jun2019}{...}
{p2coldent :* {opt for(varspec)}}lasso for {it:varspec}; {cmd:ds}, {cmd:po}, and {cmd:xpo} commands only{p_end}
{p2coldent :* {opt xfold(#)}}lasso for the {it:#}th cross-fit fold; {cmd:xpo} commands only{p_end}
{p2coldent :* {opt resample(#)}}lasso for the {it:#}th resample; {cmd:xpo} commands with {cmd:resample} only{p_end}
