{* *! version 1.0.0  11feb2015}{...}
{phang}
{opt vce(vcetype)} specifies the type of standard error reported,
which includes types that are derived from asymptotic theory ({opt oim}),
that are robust to some kinds of misspecification ({opt robust}),
that allow for intragroup correlation ({opt cluster} {it:clustvar}), and that
use bootstrap or jackknife methods ({opt bootstrap}, {opt jackknife}); see
{manhelpi vce_option R}.{p_end}
